apiVersion: v1
kind: ConfigMap
metadata:
  name: payment-config
data:
  application-k8s.properties: |
    spring.rabbitmq.host=rabbitmq
    spring.rabbitmq.port=5672
    spring.rabbitmq.username=guest
    spring.rabbitmq.password=guest

    # Replace with actual service URLs as per K8s naming
    order.service.url=http://order-service
    stripe.secret.key=sk_test_...

    # PostgreSQL config
    spring.datasource.url=jdbc:postgresql://ep-rough-snow-a4m8b5yw-pooler.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
    spring.datasource.username=neondb_owner
